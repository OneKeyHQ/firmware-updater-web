diff --git a/node_modules/@onekeyfe/hd-core/dist/index.js b/node_modules/@onekeyfe/hd-core/dist/index.js
index fbcee73..6f1a9ad 100644
--- a/node_modules/@onekeyfe/hd-core/dist/index.js
+++ b/node_modules/@onekeyfe/hd-core/dist/index.js
@@ -40716,7 +40716,14 @@ function parseProtocolV2Resources(value) {
         },
     };
 }
-const PROTOCOL_V2_RESOURCE_DEVICE_ROOTS = ['vol0:/bundles/', 'vol0:/loaders/rom/'];
+const PROTOCOL_V2_RESOURCE_DEVICE_ROOTS = [
+    'vol0:/bundles/',
+    'vol0:/loaders/rom/',
+    'vol0:/loaders/bootloader/',
+];
+function isProtocolV2ResourcePackagePath(path) {
+    return path.endsWith('.okpkg') || path.endsWith('.okpkg.staging');
+}
 function isAllowedResourceDevicePath(path) {
     if (path.includes('\\') ||
         path.includes('//') ||
@@ -40727,10 +40734,8 @@ function isAllowedResourceDevicePath(path) {
         path.split('/').some(part => part === '.' || part === '..')) {
         return false;
     }
-    if (path === PROTOCOL_V2_BOOT_RESOURCE_PACKAGE_STAGING_PATH) {
-        return true;
-    }
-    return (path.endsWith('.okpkg') && PROTOCOL_V2_RESOURCE_DEVICE_ROOTS.some(root => path.startsWith(root)));
+    return (isProtocolV2ResourcePackagePath(path) &&
+        PROTOCOL_V2_RESOURCE_DEVICE_ROOTS.some(root => path.startsWith(root)));
 }
 function readAscii(bytes, offset, length) {
     return Array.from(bytes.slice(offset, offset + length))
@@ -51906,8 +51911,12 @@ const PROTOCOL_V2_REMOTE_COMPONENT_TARGETS = {
     },
 };
 const PROTOCOL_V2_FIRMWARE_STAGING_PATHS = new Set(Object.values(PROTOCOL_V2_REMOTE_COMPONENT_TARGETS).map(target => `${PROTOCOL_V2_FIRMWARE_STAGING_VOLUME}${target.fileName}`));
-const isProtocolV2BootResourcePackagePath = (devicePath) => typeof devicePath === 'string' &&
-    devicePath.toLowerCase() === PROTOCOL_V2_BOOT_RESOURCE_PACKAGE_STAGING_PATH;
+const isProtocolV2BootResourcePackagePath = (devicePath) => {
+    if (typeof devicePath !== 'string')
+        return false;
+    const normalized = devicePath.toLowerCase();
+    return (normalized.startsWith('vol0:/loaders/bootloader/') && normalized.endsWith('.okpkg.staging'));
+};
 const PROTOCOL_V2_UPDATE_TARGET_BY_TARGET_ID = new Map([
     [ProtocolV2FirmwareTargetType.FW_MGMT_TARGET_BOOTLOADER, 'boot'],
     [ProtocolV2FirmwareTargetType.FW_MGMT_TARGET_APPLICATION_P1, 'app_v1'],
diff --git a/node_modules/@onekeyfe/hd-core/src/api/FirmwareUpdateV4.ts b/node_modules/@onekeyfe/hd-core/src/api/FirmwareUpdateV4.ts
index 613bae4..49e100a 100644
--- a/node_modules/@onekeyfe/hd-core/src/api/FirmwareUpdateV4.ts
+++ b/node_modules/@onekeyfe/hd-core/src/api/FirmwareUpdateV4.ts
@@ -301,9 +301,13 @@ const PROTOCOL_V2_FIRMWARE_STAGING_PATHS = new Set(
   )
 );
 
-const isProtocolV2BootResourcePackagePath = (devicePath: string) =>
-  typeof devicePath === 'string' &&
-  devicePath.toLowerCase() === PROTOCOL_V2_BOOT_RESOURCE_PACKAGE_STAGING_PATH;
+const isProtocolV2BootResourcePackagePath = (devicePath: string) => {
+  if (typeof devicePath !== 'string') return false;
+  const normalized = devicePath.toLowerCase();
+  return (
+    normalized.startsWith('vol0:/loaders/bootloader/') && normalized.endsWith('.okpkg.staging')
+  );
+};
 
 const PROTOCOL_V2_UPDATE_TARGET_BY_TARGET_ID = new Map<
   number,
diff --git a/node_modules/@onekeyfe/hd-core/src/protocols/protocol-v2/resources.ts b/node_modules/@onekeyfe/hd-core/src/protocols/protocol-v2/resources.ts
index 74324f1..154d763 100644
--- a/node_modules/@onekeyfe/hd-core/src/protocols/protocol-v2/resources.ts
+++ b/node_modules/@onekeyfe/hd-core/src/protocols/protocol-v2/resources.ts
@@ -49,7 +49,15 @@ export function parseProtocolV2Resources(value: unknown): IProtocolV2Resources |
   };
 }
 
-const PROTOCOL_V2_RESOURCE_DEVICE_ROOTS = ['vol0:/bundles/', 'vol0:/loaders/rom/'] as const;
+const PROTOCOL_V2_RESOURCE_DEVICE_ROOTS = [
+  'vol0:/bundles/',
+  'vol0:/loaders/rom/',
+  'vol0:/loaders/bootloader/',
+] as const;
+
+function isProtocolV2ResourcePackagePath(path: string): boolean {
+  return path.endsWith('.okpkg') || path.endsWith('.okpkg.staging');
+}
 
 function isAllowedResourceDevicePath(path: string): boolean {
   if (
@@ -63,11 +71,9 @@ function isAllowedResourceDevicePath(path: string): boolean {
   ) {
     return false;
   }
-  if (path === PROTOCOL_V2_BOOT_RESOURCE_PACKAGE_STAGING_PATH) {
-    return true;
-  }
   return (
-    path.endsWith('.okpkg') && PROTOCOL_V2_RESOURCE_DEVICE_ROOTS.some(root => path.startsWith(root))
+    isProtocolV2ResourcePackagePath(path) &&
+    PROTOCOL_V2_RESOURCE_DEVICE_ROOTS.some(root => path.startsWith(root))
   );
 }
 
